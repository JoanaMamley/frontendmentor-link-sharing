import { LinksSortPipe } from './links-sort.pipe';

describe('LinksSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LinksSortPipe();
    expect(pipe).toBeTruthy();
  });
});
